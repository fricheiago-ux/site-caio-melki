.DS_Store

# Material de referência (sites de terceiros clonados para extrair ideias de
# design no início do projeto). Nunca faz parte do site publicado, e não
# precisa de histórico de versão — fica só localmente.
# Movida para cá em 22/09/2026 (reorganização de pastas): antes era
# assets/templates/, mas ela nunca fez parte de assets/, que é só o que o
# site publica — _nao-publicar/ deixa isso explícito no próprio nome.
_nao-publicar/templates/

# Permissões locais do Claude Code nesta máquina — não é segredo, mas é
# configuração de máquina, não do projeto.
.claude/settings.local.json
